import React from 'react';
import { Button } from '@/shared/components/ui/button';
interface HomePropsType {}

const Home = ({}: HomePropsType) => {
  return (
    <main>
      <Button>dsfdsfdsa</Button>
    </main>
  );
};

export default Home;
